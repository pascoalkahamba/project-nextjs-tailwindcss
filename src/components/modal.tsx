import React from "react";

const Modal = () => {
  return (
    <section className="fixed w-full h-full flex">
      <div className="w-16 justify-center items-center p-4 rounded-lg bg-slate-200">
        <p>Do you sure to do this option?</p>
      </div>
    </section>
  );
};

export default Modal;
